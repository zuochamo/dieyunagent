{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Webhook 配置",
  "properties": {
    "webhookUrl": {
      "type": "string",
      "title": "Webhook URL",
      "description": "接收 JSON POST 的完整 URL",
      "format": "uri"
    },
    "notifyOnPlanRan": {
      "type": "boolean",
      "title": "定时任务完成后通知",
      "description": "启用后，定时计划执行结束会向 Webhook 发送摘要",
      "default": false
    }
  },
  "required": ["webhookUrl"]
}
