---
description: dieyunagent 仓库开发边界与验证命令（AI Agent 专用）
alwaysApply: true
---

# dieyunagent 开发规则

完整指南见根目录 [AGENTS.md](../../AGENTS.md) 与 [docs/AGENT-DEV.md](../../docs/AGENT-DEV.md)。

## 必守原则

- **最小 diff**：只改任务相关文件；不顺手重构、不扩 scope。
- **禁用意图关键词硬编码**：勿用中英文词表/寒暄正则路由「是否注入上下文 / AGENTS 章节 / 是否建索引」；用结构信号（@Codebase、路径、编辑器选区）、召回命中、或 `agent-limits` 配置。检索侧对用户查询做分词打分除外。
- **分层**：Renderer = UI；对话 LLM 在 Main/Node；Rust `dieyun-core` 是循环账本 / planner / memory / index，不是对话 LLM。工具走 Gateway RPC。勿在 Renderer 或 Rust 循环里堆工头协议。
- **循环**：tool_calls = 继续；无工具文字 = 本轮结束。有正文即交付；空回复才 synthesis，放在 Node `rust-loop-runner`。完成验收只提示、不自动续跑。允许重复工具止损、总步数、假完成（声称写入却未写）。
- **护栏数字**：只改 `src/agent/agent-limits.js`，勿在别处硬编码新上限。
- **不提交**：密钥、`.env`、`deploy.local.json`、`dist/`、`release/`。
- **验证**：改完跑 `npm test`（按 diff）或 `npm run test:agent`（全量 smoke）。

## 改什么去哪

| 需求 | 路径 |
|------|------|
| 聊天气泡 / 思考区 | `src/renderer/renderer-chat-render.js`、`renderer-thinking-trace.js` |
| Agent 循环 / trace | `crates/dieyun-core/src/agent/`、`src/agent/rust-loop-runner.js` |
| 模型可见规则 | `src/agent/agent-system-prompt.js`、`system-prompt-prep.js` |
| 工具 schema / 执行 | `src/agent/tool-catalog.js`、`tool-bridge-main.js`、`src/gateway/fs-edit-file.js` |
| Gateway / RPC | `src/gateway/`、`crates/dieyun-core/src/rpc/` |
| 手机端 | `src/mobile/public/`、`src/mobile/bridge.js`、`src/agent/service.js` |
| 护栏 | `src/agent/agent-limits.js`、`guardrails-shared.js` |

模块索引与测试映射：[docs/CODEMAP.json](../../docs/CODEMAP.json)。Renderer 模块表：[src/renderer/README.md](../../src/renderer/README.md)。

## 开发命令

- **日常**：`npm run dev:fast`（跳过 remote-gateway pack）
- **完整**：`npm run dev`（SSH/WSL 远程或 pack 过期时）
- **清理后**：`powershell -File scripts/agent-bootstrap.ps1`

## 清理后恢复

```bash
npm ci && npm run bootstrap && npm run pack:dieyun-core
```
