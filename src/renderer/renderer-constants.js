/* Shared numeric limits loaded before chat/agent modules */
'use strict';

const TRACE_DESKTOP_THOUGHT_CHARS = 2000;
/** 新轮次 / 工具状态变化 */
const MOBILE_TRACE_PUSH_INTERVAL_MS = 100;
/** 同一轮内流式文字更新（刷屏：更密推送） */
const MOBILE_TRACE_STREAM_INTERVAL_MS = 20;

